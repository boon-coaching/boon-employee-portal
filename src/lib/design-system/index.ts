// Vendored from ~/boon-design-system/components. This is a temporary mirror
// until @boon/design-system ships as a proper package. When that happens,
// delete this directory and switch imports back to '@boon/design-system'.
//
// Only the 5 primitives currently in product use are vendored here. If you
// need another primitive (Input, Textarea, Modal, Tabs, etc.), copy its
// .tsx file from ~/boon-design-system/components and add the export below.
export { Card } from './Card'
export { Headline } from './Headline'
export { Button } from './Button'
export { Avatar } from './Avatar'
export { Badge } from './Badge'
